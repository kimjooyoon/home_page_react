import {TextFieldProps} from "@mui/material";
import TextField from "@mui/material/TextField";

const TextFieldAtom = (props: TextFieldProps) => {
  return <TextField {...props} />;
}

export default TextFieldAtom;
